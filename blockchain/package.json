{
  "name": "NexLogica-blockchain",
  "version": "1.0.0",
  "description": "Blockchain smart contracts for NexLogica",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "lint": "solhint 'contracts/**/*.sol'",
    "build": "hardhat compile"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "solidity",
    "hardhat",
    "polygon"
  ],
  "author": "NexLogica",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.19.0",
    "solhint": "^4.0.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}